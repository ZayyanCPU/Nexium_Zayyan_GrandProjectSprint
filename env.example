# Supabase Configuration
# Get these from your Supabase project dashboard
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# MongoDB Configuration (optional - for additional data storage)
MONGODB_URI=your_mongodb_connection_string_here

# n8n Workflow Configuration
# This is the webhook URL from your n8n workflow for AI recipe generation
# If not configured, the app will use fallback recipe generation
N8N_WEBHOOK_URL=your_n8n_webhook_url_here

# OpenAI Configuration (for n8n workflow)
# This API key is used by the n8n workflow, not directly by this app
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Database URL for direct database connection
DATABASE_URL=your_database_url_here 